<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/pref_exchange_pair_key"
        android:title="@string/pref_exchange_pair_title"
        android:summary="%s"
        android:defaultValue="@string/pref_exchange_pair_default_value"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
