<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory
        android:title="@string/pref_category_exchanges_title"
        android:key="@string/pref_category_exchanges_title"
        >

        <ListPreference
            android:key="@string/pref_exchange_key"
            android:title="@string/pref_exchange_title"
            android:summary="%s"
            android:defaultValue="@string/pref_exchange_default_value"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />

    </PreferenceCategory>

    <PreferenceCategory
        android:title="@string/pref_category_pairs_title"
        android:key="@string/pref_category_pairs_key"
    >

    </PreferenceCategory>



</PreferenceScreen>
